<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Jobiz - Votre plateforme d'emploi IT{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>💼</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        
        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="min-h-screen bg-gray-100 font-sans">
        <nav class="bg-white shadow-lg">
            <div class="max-w-7xl mx-auto px-4">
                <div class="flex justify-between h-16">
                    <div class="flex">
                        <div class="flex-shrink-0 flex items-center">
                            <a href="{{ path('app_home') }}" class="flex items-center gap-2">
                                <img src="{{ asset('assets/logo-jobiz.png') }}" alt="Logo Jobiz" class="h-10 w-auto">
                                <span class="text-2xl font-bold text-slate-700">Jobiz</span>
                            </a>
                        </div>
                        <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
                            <a href="{{ path('app_home') }}" class="inline-flex items-center px-1 pt-1 text-teal-600 font-semibold">Accueil</a>
                            <a href="{{ path('app_jobs') }}" class="inline-flex items-center px-1 pt-1 text-slate-700 hover:text-teal-600">Offres</a>
                            <a href="{{ path('app_about') }}" class="inline-flex items-center px-1 pt-1 text-slate-700 hover:text-teal-600">À propos</a>
                        </div>
                    </div>
                    <div class="flex items-center">
                        {% if app.user %}
                            <a href="{{ path('app_logout') }}" class="text-slate-700 hover:text-teal-600">Déconnexion</a>
                        {% else %}
                            <a href="{{ path('app_login') }}" class="text-slate-700 hover:text-teal-600">Connexion</a>
                        {% endif %}
                    </div>
                </div>
            </div>
        </nav>

        <main class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
            {% block body %}{% endblock %}
        </main>

        <footer class="bg-white mt-8">
            <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
                <p class="text-center text-gray-500">&copy; 2024 Jobiz. Tous droits réservés.</p>
            </div>
        </footer>
    </body>
</html>
