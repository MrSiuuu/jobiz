{% extends 'base.html.twig' %}

{% block title %}Accueil - Jobiz{% endblock %}

{% block body %}
    <div class="bg-white">
        <div class="max-w-7xl mx-auto py-16 px-4 sm:py-24 sm:px-6 lg:px-8">
            <div class="text-center">
                <h1 class="text-4xl tracking-tight font-extrabold text-slate-700 sm:text-5xl md:text-6xl">
                    <span class="block">Trouvez votre</span>
                    <span class="block text-teal-600">emploi idéal en IT</span>
                </h1>
                <div class="flex justify-center mt-6 mb-2">
                    <img src="{{ asset('assets/logo-jobiz.png') }}" alt="Logo Jobiz" class="h-16 w-auto">
                </div>
                <p class="mt-3 max-w-md mx-auto text-base text-slate-700 sm:text-lg md:mt-5 md:text-xl md:max-w-3xl">
                    Jobiz est la plateforme qui connecte les meilleurs talents IT aux entreprises innovantes. Découvrez des opportunités de carrière passionnantes dans le monde de la technologie.
                </p>
                <div class="mt-5 max-w-md mx-auto sm:flex sm:justify-center md:mt-8">
                    <div class="rounded-md shadow">
                        <a href="{{ path('app_jobs') }}" class="w-full flex items-center justify-center px-8 py-3 border border-transparent text-base font-medium rounded-md text-white bg-teal-600 hover:bg-teal-700 md:py-4 md:text-lg md:px-10">
                            Voir les offres
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="bg-gray-50">
        <div class="max-w-7xl mx-auto py-12 px-4 sm:px-6 lg:py-16 lg:px-8">
            <h2 class="text-3xl font-extrabold text-slate-700 sm:text-4xl">
                Dernières offres
            </h2>
            <div class="mt-6 grid gap-6 grid-cols-1 sm:grid-cols-2 lg:grid-cols-3">
                {% for job in latest_jobs %}
                    <div class="bg-white overflow-hidden shadow rounded-lg border border-slate-100">
                        <div class="px-4 py-5 sm:p-6">
                            <h3 class="text-lg font-medium text-slate-700">{{ job.title }}</h3>
                            <p class="mt-1 text-sm text-teal-600">{{ job.company.name }}</p>
                            <div class="mt-2 flex flex-wrap gap-2">
                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-teal-100 text-teal-800">
                                    {{ job.type.name }}
                                </span>
                            </div>
                            <div class="mt-4">
                                <a href="{{ path('app_job_show', {id: job.id}) }}" class="text-teal-600 hover:text-teal-800 font-semibold">
                                    Voir l'offre →
                                </a>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %} 